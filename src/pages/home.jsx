import React from 'react';
import Loadapp from '../components/loadApp';

class Home extends React.Component {
    render() {
        return (
            <Loadapp />
        );
    }
}

export default Home;
